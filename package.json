{
  "name": "animal-arcade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ten tablet mini games for one or two players, age five and up.",
  "scripts": {
    "start": "http-server -p 8899 -s .",
    "test:ai": "node tests/ai-strength.mjs",
    "test:smoke": "node tests/smoke.mjs",
    "test:e2e": "node tests/e2e.mjs"
  },
  "//": "The app itself has no runtime dependencies and no build step. These are development tools only: a static server to run it, and a browser to test it in.",
  "devDependencies": {
    "http-server": "^14.1.1",
    "playwright": "^1.63.0"
  }
}
